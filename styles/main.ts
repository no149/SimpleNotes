import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  mainContainer: {
    padding: 10,

    flex: 1,
    flexDirection: 'column',
  },
  navbar: { flexDirection: 'row', marginBottom: 10, justifyContent: 'center' },
  mainContent: { flex: 1, height: '100%' },
})
