import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  mainContainer: { padding: 10, height: '100%' },
  navbar: { flexDirection: 'row', marginBottom: 10, justifyContent: 'center' },
  mainContent: {},
})
