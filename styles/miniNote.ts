import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  listItem: {
    borderBottomColor: 'black',
    alignItems: 'center',
    borderBottomWidth: 1,
    borderColor: 'black',
  },
  listItemHeader: { fontWeight: 'bold', fontSize: 18 },
})
